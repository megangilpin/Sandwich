<h1>Which Sandwich!?</h1>

<h2>Sandwiches to eat!</h2>

<ul>
  {{#each sandwiches}}
    {{#unless devoured}}
      {{> sandwiches/sandwich-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Sandwiches that have been DEVOURED!</h2>

<ul>
  {{#each sandwiches}}
    {{#if devoured}}
      {{> sandwiches/sandwich-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Sandwich</h2>
<form class="create-form">

  <div class="form-group">
    <label for="sw">Sandwich Name:</label>
    <input type="text" id="sw" value="0" name="sandwich_name">
  </div>
{{!-- 
  <div class="form-group">
    <label for="devoured">Eat Me?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eaten!<br>
    <input type="radio" name="devoured" value="0"> Eat me!
  </div> --}}

  <button type="submit">Add Sandwich</button>
</form>
